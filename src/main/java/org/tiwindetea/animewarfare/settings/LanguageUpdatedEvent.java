package org.tiwindetea.animewarfare.settings;

import org.lomadriel.lfc.event.Event;

public class LanguageUpdatedEvent implements Event<LanguageUpdatedEventListener> {
	@Override
	public void notify(LanguageUpdatedEventListener listener) {
	}
}
