package org.tiwindetea.animewarfare.net.logicevent;

import java.util.EventListener;

public interface FlyingStudioZoneChoiceEventListener extends EventListener {
	void handleZoneChoice(FlyingStudioZoneChoiceEvent event);
}
